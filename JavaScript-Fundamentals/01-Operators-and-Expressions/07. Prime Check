function solve(args) {
    var num=+args[0];
    if (num < 2) {
        return false;
    } else {
        var limit = Math.sqrt(num)
       
        for (var i = 2; i <= limit; i++) {
            if (num%i==0){
                return false;
            }
        }
        return true;
    }
}
