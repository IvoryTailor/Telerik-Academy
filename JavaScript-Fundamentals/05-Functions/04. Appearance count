function solve(args) {
    var arr = args[0].split('\n');
    var size = +arr[0];
    var number = +arr[2];
    var repeat = 0;
    arr = arr[1].split(' ');
    for (var i = 1; i < size - 1; i++) {
        if (+arr[i] === number) {
            repeat++;
        }
    }
    return repeat;
}
