function solve(args) {
    var leng = +args[0];
    var arr = args[1].split(' ');
    if (arr.length > 1) {
        console.log(arr.sort(function (a, b) {
            return a - b;
        }).join(' '));
    }
}
