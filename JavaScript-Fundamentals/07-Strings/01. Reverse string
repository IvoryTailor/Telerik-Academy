function solve(args) {
    var word = args[0].split('');
    var reversed = new String;
    for (var i = word.length-1; i >= 0; i-=1) {
        reversed += word[i];
    }
    console.log(reversed);
}
